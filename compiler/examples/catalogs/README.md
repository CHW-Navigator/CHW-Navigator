# Catalog Examples

These files show the standalone authoring inputs that `chw-nav compose-ir` can ingest directly:

- `pneumonia.metadata.json`
- `pneumonia.variables.csv`
- `pneumonia.predicates.json`
- `pneumonia.phrases.csv`

The phrase bank uses `text_<language>` columns so the same phrase row can carry multiple languages.
