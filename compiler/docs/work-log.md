# CHW Navigator compiler work log

## WS0 - Establish the executable baseline - 2026-08-05

**Delivered:** WS0 discovery reports and human decisions; corrected source-of-truth documentation; ADR-001; evidence semantics; a non-shrinking executable TypeScript overlap map; repaired Product baseline fixtures/contracts; a root-independent baseline verifier with structured manifest output; and GitHub issue #2 as the explicit triage lifecycle for the previously file-only Product security/reliability audit.

**Deviations:** Individual ignored environment paths were counted and classified by root rather than committed, because the common rules prohibit machine-specific scratch evidence. The old CSV `english_text` output-header assertion was explicitly superseded by canonical `text` output while retaining and testing legacy `english_text` input compatibility.

**Defects found:** Ambiguous repository roots and stale submodule/readiness claims; four Product baseline failures; two Product pytest collection warnings; one compiler official-harness skip; four Windows TypeScript symlink-security skips; a Windows Python-copy failure on stale generated oracle staging paths; unused Prompt B text; mini-manuals that do not test Prompt B; and no Product clinical-logic to canonical-IR adapter.

**Root cause:** Repository copies and prose claims were not bound to one commit/evidence manifest. The Product fixture, converter expectations, and tests were committed without an enforced combined root gate. Framework skip output was not elevated into release semantics. Product and compiler IR contracts evolved independently.

**Generalized guardrail:** `verify_repository_baseline.py` executes and recounts the compiler, Product, and disposable-copy TypeScript suites; uses Windows-native copy semantics for the oracle and converts copy failures into structured results; preserves every non-pass status; checks source lock, source-truth phrases, overlap-map invariants, work-log completeness, dirty state, and archived-evidence misuse; and fails release mode for dirty source, required skips, warnings, or other non-pass results. Focused tests assert copier return-code semantics, activator-module closure, router/activator policy selection, and legacy-input/canonical-output CSV behavior. File-only audit findings are now linked to GitHub issue #2, where each must receive an evidence-backed disposition or a focused child issue.

**Status ledger:** Combined executable baseline: 849 pass, 0 fail, 5 skipped, 0 not_run across 854 tests. Compiler: 149 pass and 1 skipped; Product: 85 pass with no warnings; TypeScript oracle: 615 pass and 4 skipped. Human WS0 decisions: pass. Exact E4/E5 checks remain not_run and E6 approval remains not_supplied.

**Evidence level earned:** E0 overall. All executable unit suites have zero failures, but one official local-data harness case and four TypeScript filesystem-security cases are skipped; mandatory non-pass results cannot earn E1 in the combined manifest.

**Blocked on:** E3 local-data harness execution requires a working `xsltproc`; four TypeScript symlink-security cases require a non-Windows required CI job or Windows-native equivalent; E4-E6 target-runtime, offline/server, clinical, governance, security, and deployment evidence remains external.

## WS1 - Minimum viable contracts - 2026-08-05

**Delivered:** Content-addressed registry-set, capability-registry, and exact CHT target-profile schemas; a strict Python loader/resolver; a sealed tracer fixture and mutation-based negative corpus; stable `CHWN-REG-*`, `CHWN-TARGET-*`, and `CHWN-SCOPE-*` diagnostics; and the three named WS1 test modules. Capability fields are limited to the WS1 list, unknown fields fail closed, input order is preserved, output bindings are restricted to `technical.*`, and Release 1 accepts only `current_contact`.

**Deviations:** The resolvable evidence state is named `tracer_enabled`, not `verified` or `approved`. It enables only the hand-written WS2 contract tracer and deliberately makes no implementation, clinical, governance, or deployment claim. The planned `calculate_gestational_age_naegele` implementation binding is recorded but is not supplied in WS1; implementing and executing it belongs to WS2.

**Defects found:** The first draft used `verified` before a matching WS2 implementation existed, which would have inflated evidence. The initial target-feature resolver considered local-data features but not extension support. Hand-maintained JSON Schemas and runtime models also had no parity check and could have drifted independently.

**Root cause:** Contract resolvability, implementation evidence, and approval were represented too close together, and target capabilities were split across two fields without one resolution view. Stored schemas and runtime validation were separate artifacts without a shared executable invariant.

**Generalized guardrail:** Candidate entries always parse but never resolve; the only WS1 resolvable state is explicitly non-approval `tracer_enabled`. Resolution combines declared local-data and enabled extension features, rejects unsupported profiles, and emits stable diagnostics. Tests compare schema root fields with runtime models, assert the exact capability surface, derive the set digest independently from named member digests, mutate capability/target/input-order sources, run every negative fixture, and exercise every declared diagnostic.

**Status ledger:** WS1 focused suite: 16 pass, 0 fail, 0 skipped. Full compiler suite: 164 pass, 0 fail, 1 environment-dependent official-harness skip across 165 tests. No Product or TypeScript behavior was changed by WS1.

**Evidence level earned:** E1 for the isolated WS1 deterministic unit/negative-fixture suite. The repository-wide evidence floor remains E0 because the pre-existing official local-data harness check is skipped on this Windows environment.

**Blocked on:** Nothing within WS1. WS2 must implement the planned binding, execute the tracer through generated artifacts and the local harness, and keep any skipped or unavailable runtime evidence explicit.

## WS2 - Early tracer bullet - 2026-08-05

**Delivered:** A hand-written, no-AI tracer from canonical Clinical IR through content-addressed capability resolution into a CHT 5.2 form/task bundle; a registered `current_contact` read of the most recent LMP with missing/stale handling; the deterministic `technical.gestational-age.naegele` Python and CHT extension implementations; explicit caller branches for all eight registered statuses; typed `technical.ga_weeks`, `technical.ga_days_remainder`, and `technical.edd` binds; a single-contact report task whose seven-day interval is resolved from an integer decision-table output; content-preserving, idempotent composition into an unrelated `tasks.js` fixture with exact rollback bytes; hand-derived calendar vectors; a locked TypeScript differential oracle; a local Node/XForm structural harness; and a deterministic E2 evidence manifest whose environment section separately records the exact CHT sandbox as `not_run`.

**Deviations:** The TypeScript handoff is retained as a differential oracle through Release 1 rather than ported into or invoked by the production Python runtime. The local harness verifies generated XForm wiring and executes the emitted extension module, but it is not the official form runner or an exact CHT 5.2 sandbox; therefore it earns only E2. The clinical endpoint and interval are explicitly synthetic tracer policy, not ministry- or clinician-approved care guidance.

**Defects found:** Canonical IR had no implementation-independent capability invocation, so a model could not name a registered function without leaking Python or CHT details. Task timing was initially stored directly on the task action rather than produced by the decision table. Calculated technical outputs defaulted to string XForm binds. The reviewed TypeScript composer requires trusted prior-composition state for a second pass. Windows newline translation initially made the recorded composed hash differ from the written artifact. The tracer runner initially located the external TypeScript handoff without re-verifying the source-lock digests, and its first local harness executed the extension without checking the generated form wiring.

**Root cause:** The registry, IR, form, and task contracts had previously been tested mostly within their own layers, so the missing cross-layer references and serialization invariants were invisible. Recomposition and oracle reuse also depended on state/version assumptions that were present in the reviewed TypeScript tests but not represented in the first Python bridge.

**Generalized guardrail:** `invoke_capability` carries only a capability ID, ordered variable arguments, status target, and output mappings; registry resolution supplies implementation bindings. Stable `CHWN-RESOLVE-*` diagnostics and negative tests cover unresolved and malformed invocations. Every registered status must appear in an explicit caller branch. Static task lowering may reference a typed decision output only when every triggering rule resolves to one deterministic non-negative interval; missing or ambiguous policy fails with `CHT-TASK-003`. XForm calculate rows can preserve explicit bind types. The bridge re-verifies every locked TypeScript source digest, rejects errors on either composition pass, writes composed bytes with explicit LF newlines, compares rollback bytes and unrelated source bytes, and keeps comparable versus non-comparable oracle cases separate. Two clean builds must have identical deterministic manifest sections.

**Status ledger:** WS2 focused tracer/composition suite: 13 pass, 0 fail, 0 skipped. Full compiler suite: 178 pass, 0 fail, 1 environment-dependent official-harness skip across 179 tests. The local Node/XForm tracer harness passes. Five tracer vectors are comparable with the TypeScript oracle and pass after normalization; two invalid/out-of-domain vectors are counted separately as not comparable. Task recomposition is byte-identical and rollback exactly matches the input fixture.

**Evidence level earned:** E2 for the tracer. `deployment_ready` is false. Successful bundle generation, non-empty JSON, local Node execution, and TypeScript agreement are not CHT deployment evidence.

**Blocked on:** Exact CHT 5.2 sandbox execution is E4 external evidence and remains `not_run`; the existing official local-data harness is skipped on this Windows environment because `xsltproc` is not executable. Representative-device offline/sync, battery, memory, accessibility, and translation checks remain E5 external work, and clinical/governance/deployment approval remains E6 human work. Before any later offline queued-request or messaging work, topology and registry references must be re-resolved against a current snapshot; WS2 does not decide that staleness policy. Group-scoped obligations remain outside the `current_contact` model and require a separate design or pilot descope. Native WFA remains non-comparable until the deployment-controlled chart document bytes and version are supplied.

## WS3 - Governed registry release - 2026-08-05

**Delivered:** Tracer-informed WS3-WS9 Codex work specifications; a versioned v2 registry set that leaves the executable WS1/WS2 capability contract unchanged; a data dictionary with identifiers, value-set, type/unit/cardinality, requiredness, owner role, retention/consent/access references, interoperability mappings, provenance, and lifecycle; a capability-governance sidecar bound to exact executable capability and data-concept digests; detached clinical, data-governance, and technical attestation contracts; a separately content-addressed release contract; fail-closed activation requiring exact set/release/attestation digests, three distinct roles and people, approved decisions, current lifecycle, current timestamps, and a caller-supplied signature verifier; an unpopulated human approval template; sealed synthetic positive and negative fixtures; and stable `CHWN-REG-007` through `CHWN-REG-015` diagnostics.

**Deviations:** Production signature verification is represented by a required verifier boundary rather than an invented trust store, key, or signing service. All executable approval evidence uses visibly `synthetic-test-*` identities. Real owners, policy references, organizations, keys, signatures, jurisdiction, effective dates, and approval decisions remain unset. Registry-set v1 remains supported for the WS2 tracer but is mechanically non-activatable.

**Defects found:** The first governed shape included a data dictionary without binding capability parameters to its concepts, which would have made the dictionary ornamental. The first activation path trusted callers to have used strict parsers and could therefore accept directly constructed models without recomputing their digests. The first role check did not prohibit one person from filling multiple required roles or future-dated attestations. Adding exact concept bindings immediately found a real synthetic contract mismatch: the data concept used unit `date` while the executable capability used `calendar_date`.

**Root cause:** Previously independent capability, concept, and approval objects shared human-readable names but no mandatory cross-artifact digest/type/unit invariant. Trust was placed at parser entry points instead of re-established at the irreversible activation boundary. Role labels were checked as a set without checking the identity and time semantics of each attestation.

**Generalized guardrail:** A governed capability must bind each declared concept use to an exact concept ID, version, digest, direction, and parameter; the compiler checks that parameter and concept types and units agree. Every executable capability in a v2 set requires governance. Activation reparses and rehashes the registry set, release, and every attestation regardless of object provenance; requires exact attestation-set equality; rejects missing/duplicate roles, duplicate approver identities, rejected/unverified/expired/future-dated attestations, inactive members, superseded releases, and v1 sets; and has no default signature verifier. Schema/runtime field parity, unknown-field rejection, digest mutation, cross-binding mutation, direct-model tampering, and every diagnostic are automated tests.

**Status ledger:** WS3 data-dictionary and governance suite: 14 pass, 0 fail, 0 skipped. Full compiler suite: 192 pass, 0 fail, 1 environment-dependent official-harness skip across 193 tests. Human approval: `not_supplied`. Production signature trust service: `not_supplied`. Exact target/runtime and representative-device evidence: `not_run`.

**Evidence level earned:** E1 for deterministic schemas, digests, negative cases, and synthetic activation tests. The synthetic fixture proves fail-closed mechanics only; it is not an approval record. E6 remains `not_supplied`.

**Blocked on:** `[HUMAN INPUT]` must supply actual policy references, named owners, jurisdiction, effective interval, three independent approvers, organizations, signing-key IDs, and decisions. A production trust configuration must verify detached signatures. Exact CHT runtime, device/offline, accessibility/translation, security/privacy, operations, and deployment evidence remains external. None of these blockers prevents WS4 prompt evaluation or WS5 deterministic bridge development, but no governed production release can activate without them.

## WS4 - Prompt B candidate-needs evaluation - 2026-08-05

**Delivered:** A registry-blind Prompt B request builder; a strict `capability-needs@1.0.0` schema and parser; an explicit injected model-adapter invocation path; exact source-location, quotation, digest, local-ID, type/unit, status, subject-scope, uncertainty, and unknown-field checks; ten synthetic mini-manuals with committed recorded outputs and withheld expected annotations; deterministic precision, recall, grounding, unsupported-inference, and ambiguity evaluation; an opt-in live-model runner whose absent adapter is `not_run`; and a separate human-review input for the older operational resolver.

**Deviations:** Prompt B is not wired into the normal Gen 8 clinical run because WS4 is an optional authoring evaluation, not a new mandatory clinical stage. Callers explicitly inject a model adapter or supply human-authored candidate-needs data through the same parser. The live runner has no default provider or credentials. No live model was invoked in this work specification.

**Defects found:** The prior Prompt B existed only as an unused string. Its older capability-candidate schema allowed unknown fields and an inline `review_approval`, while the resolver treated that model-carried value as review evidence. Existing end-to-end mini-manuals exercised the clinical pipeline but never Prompt B, so they could not measure candidate grounding or hallucination. Full regression also exposed a byte-digest fixture stored as CRLF while its registered digest described LF bytes, making the special-function integrity test fail on this Windows checkout.

**Root cause:** Candidate authoring, approval, and registry-shaped resolution fields had been represented too close together without an executable request boundary. Prompt presence was mistaken for prompt use, and broad end-to-end fixtures were mistaken for stage-specific evaluation. Separately, a byte-exact artifact had no repository line-ending policy even though its digest was platform-independent.

**Generalized guardrail:** Prompt B's public functions have no registry or implementation parameter; request construction excludes expected answers; the manual is a distinct untrusted-data field; the output schema has no approval, activation, implementation, or resolved-ID surface; parser mutations reject extra fields, invented quotes and registry-shaped IDs, malformed units, and mismatched digests; no-need and adversarial manuals must remain empty; and live evidence absence is never a pass. The legacy resolver now rejects model-carried approval and accepts reviewed candidate IDs only through a separate caller argument. `.gitattributes` pins the digest-bearing golden vector to LF so byte evidence is stable across checkouts.

**Status ledger:** WS4 focused suite: 32 pass plus 10 mini-manual subtests, 0 fail, 0 skipped. Full Product suite: 102 pass plus 10 mini-manual subtests, 0 fail, 0 skipped. Recorded evaluation: 10/10 cases pass all five metrics. Live-model evaluation: `not_run`. Full compiler regression after the golden-vector checkout guardrail: 192 pass, 0 fail, and 1 environment-dependent official-harness skip across 193 tests. The combined repository gate records 909 pass, 0 fail, and 5 explicit environment-dependent skips across 914 tests: one compiler `xsltproc` harness case and four TypeScript Windows symlink cases. Its overall status is therefore `incomplete`, evidence E0, not pass. The earlier pre-fix compiler run with one digest failure and an initial combined-gate command timeout are not counted as passing evidence.

**Evidence level earned:** E1 for deterministic prompt/parser and mutation tests; E2 for the committed recorded-output evaluation. Live-model quality is `not_run` and cannot raise the evidence level. Nothing in WS4 is approval, registry activation, clinical validation, or deployment evidence.

**Blocked on:** No WS5 blocker: human-authored candidate-needs can use the strict parser if live Prompt B quality later misses threshold. A live-model adapter and environment are optional evaluation inputs and remain `not_run`; they must not receive expected annotations or registry IDs. Exact CHT runtime/device evidence and human approval remain external E4-E6 work.

## WS5 - Canonical IR bridge and deterministic resolution - 2026-08-05

**Delivered:** A bounded Product-to-canonical adapter and strict content-addressed adapter contract; accounting for all seven Product sections through canonical mappings or a stable loss report; an exact, registered CHT local-data binding; a separately reviewed semantic-needs artifact bound to the exact registry-blind WS4 candidate digest; deterministic resolution against one governed registry set, activation result, and target profile; a content-addressed resolution lock; implementation-independent capability invocation; explicit handling of every registered technical status; distinct technical and clinical output namespaces; reviewed synthetic task intent; a root `bridge-product` command; four strict JSON schemas; and a synthetic vertical slice that compiles through the WS2 CHT bundle, local harness, and differential oracle.

**Deviations:** Prompt B output does not flow directly into resolution. It cannot know registry family/operation vocabulary, deployment status names, target configuration, or Product variable IDs without violating the registry-blind control or inventing facts absent from the manual. A separate reviewed semantic binding supplies those exact normalized fields but cannot contain a capability or implementation ID. Adapter v1 supports only the variable-bearing synthetic tracer and empty clinical-logic sections; any nonempty rule/module/predicate/phrase section stops with a loss report. The committed activation result and task intent are visibly synthetic E2 fixtures, not human approval or a production trust token.

**Defects found:** The initial bridge draft trusted an arbitrary `source_candidate_digest` without receiving and rehashing the source candidate, so review provenance could not be proven. Directly constructed Pydantic models could also bypass adapter and resolution-lock digest checks at exported function boundaries. Cross-layer testing found that date variables and the local LMP binding had omitted `calendar_date` even though the governed capability required that exact unit. Product's candidate status vocabulary and parameter names are intentionally more general than the governed executable contract, so direct candidate-to-registry matching would either fail valid cases or encourage fuzzy translation.

**Root cause:** Registry-blind source extraction, deployment-specific semantic normalization, approval, and executable selection are different trust stages, but the original WS5 wording treated "candidate needs" as though one artifact could safely serve all of them. Separately, type/unit invariants and content verification had been enforced at individual parsers rather than re-established across every exported composition boundary.

**Generalized guardrail:** The compiler verifies the canonical digest of the exact WS4 candidate before conversion, reparses and rehashes reviewed needs, adapters, governed registries, and resolution locks at use boundaries, and never uses candidate prose as a selector. Resolution is equality-only over family, operation, ordered parameter names/types/units, complete status set, target profile, subject scope, and approved governance. Wording changes cannot affect selection; semantic changes fail closed; zero and multiple exact matches have distinct diagnostics. Unknown Product fields, coercible values, provenance loss, unsupported sections, unregistered local reads, mutated candidates, direct-model tampering, registry reordering, and contract mismatches are automated negative tests. Date-like registry and local-data parameters carry explicit semantic units.

**Status ledger:** WS5 focused suite: 20 pass, 0 fail, 0 skipped. Full compiler suite after WS5: 212 pass, 0 fail, and 1 environment-dependent `xsltproc` skip across 213 tests. Full Product suite: 102 pass plus 10 mini-manual subtests, 0 fail, 0 skipped. The WS5 source candidate passes the actual WS4 strict parser. The generated tracer's local Node/XForm harness and TypeScript differential oracle pass. The combined repository gate records 929 pass, 0 fail, and 5 explicit environment-dependent skips across 934 tests: the compiler `xsltproc` case and four TypeScript Windows symlink cases. Its overall status is correctly `incomplete`, evidence E0, not pass. Exact CHT sandbox execution remains `not_run`.

**Evidence level earned:** E1 for contracts, parsing, digests, diagnostics, and adversarial unit tests; E2 for the deterministic synthetic vertical slice and local/differential execution. A generated nonempty IR, passing local harness, or exact registry match is not deployment readiness.

**Blocked on:** Broader Product predicates, modules, routing, and phrase-bank conversion require explicit canonical mappings that do not infer clinical meaning. A production invocation must consume an activation result obtained from WS3 using real release artifacts, independent human attestations, and a real signature verifier; the serialized synthetic result is test input only. Exact CHT sandbox, real-handset offline/sync, performance, accessibility, translation, security/privacy, clinical validation, governance approval, and deployment authorization remain external E4-E6 work. Group-scoped tasks and stale queued/offline resolution remain deferred to their later work specifications.
